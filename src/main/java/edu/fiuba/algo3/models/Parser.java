package edu.fiuba.algo3.models;
/* 
import org.fasterxml.jackson.databind.ObjectMapper;

import org.json;*/

import java.io.BufferedReader;
import java.io.FileReader;
import java.util.Map;

/* 
public class Parser {

    public static void parser(){

        try {
            // Lee el archivo JSON
            BufferedReader br = new BufferedReader(new FileReader("ruta/al/archivo.json"));
            // Convierte el archivo JSON a un objeto Java
            Gson gson = new Gson();
            Mapa mapa = gson.fromJson(br, Mapa.class);
            
            // Accede a los datos del mapa
            Map<String, String[]> mapaCompleto = mapa.getMapa();
            String[] fila1 = mapaCompleto.get("1");
            // ...continúa accediendo a los demás datos según necesites

            // Cierra el BufferedReader
            br.close();
        } catch (Exception e) {
            e.printStackTrace();
        }
}}*/

